# Core ML Libraries
tensorflow==2.11.0
keras==2.11.0
numpy==1.23.5
pandas==2.3.1
scikit-learn==1.4.2
joblib==1.4.2

# Visualization
matplotlib==3.9.0
seaborn==0.13.2

# API Request & Caching
openmeteo-requests==1.1.0
requests==2.31.0
requests-cache==1.2.0
retry-requests==2.0.0

# Backend - FastAPI
fastapi==0.111.0
uvicorn==0.29.0
pydantic==1.10.14

# Frontend - Streamlit
streamlit==1.35.0

# Compatibility Fixes
protobuf==3.20.3


⚠️ Notes:
protobuf==3.20.3 is pinned to avoid conflicts with TensorFlow 2.11.0.
pydantic==1.10.14 is used because FastAPI currently requires Pydantic v1 (not v2) for compatibility.
openmeteo-requests is a 3rd party Open-Meteo Python wrapper that must be installed manually if you’ve been using it (as in your current code).